# Installation

This package is build with [poetry](https://python-poetry.org/) so installation is as you might expect.

* **Using `pip`**
```shell
pip install qlient
```

* **Using `poetry`**
```shell
poetry add qlient
```